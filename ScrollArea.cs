using Godot;
using System;

public partial class ScrollArea : ScrollContainer
{
	public override void _Ready()
	{
  //      Vector2 size = new Vector2(15, GetVScrollBar().CustomMinimumSize.Y);
  //      GetVScrollBar().CustomMinimumSize = size;
		//Texture2D rectangle = new Texture2D();
		//rectangle.
		//GetVScrollBar().AddThemeIconOverride("scrollIcon", rectangle);
    }

	public override void _Process(double delta)
	{
	}
}
